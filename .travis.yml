language: cpp
script:
  - cmake --build build --config Release
compiler: clang
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - libxrandr-dev
      - libxinerama-dev
      - libxcursor-dev
      - libxi-dev
      - libstdc++-7-dev
matrix:
  include:
    - os: linux
      before_script: 
        - cmake -H. -Bbuild
    - os: windows
      before_script:
        - choco install make 
        - cmake -H. -Bbuild -G"Unix Makefiles"
